📦 System Flow Breakdown
1. Slicer + Zip Maker (Python)

Open tab in browser. 
Allow drag and drop of model1.stl and model2.stl
Run slicer → get zip1/ and zip2/ (save then access?)
Interleave PNGs: slice0_model1.png, slice1_model2.png, ...
Output a final merged ZIP

- additionally -> calculate percentage overlap? calculate any gaps on the inside of the models? 

2. Print Controller Software
Open merged ZIP
At each step:
Display PNG on LCD (via microcontroller)
Move Z-axis up/down
Wait for drip / rinse
Slide to next bath (X-axis)
Repeat

3. Microcontroller (e.g., Arduino + DRV8825)
Accept commands via serial:
MOVE Z 1000 (step height)
MOVE X 5000
DISPLAY slice03.png
Handle LCD + motors in response

